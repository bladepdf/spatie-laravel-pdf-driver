name: ci

on:
    push:
        branches:
            - main
            - master
    pull_request:

jobs:
    tests:
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                include:
                    - php: '8.2'
                      testbench: '^9.0'
                      advisory_blocking: '0'
                      composer_flags: '--prefer-lowest'
                    - php: '8.3'
                      testbench: '^10.0'
                      advisory_blocking: '1'
                      composer_flags: ''
                    - php: '8.4'
                      testbench: '^11.0'
                      advisory_blocking: '1'
                      composer_flags: ''
                    - php: '8.5'
                      testbench: '^11.0'
                      advisory_blocking: '1'
                      composer_flags: ''

        name: PHP ${{ matrix.php }} / Testbench ${{ matrix.testbench }}

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: ${{ matrix.php }}
                  coverage: none
                  tools: composer:v2

            - name: Configure Testbench version
              run: composer require --dev --no-interaction --no-update orchestra/testbench:${{ matrix.testbench }}

            - name: Install dependencies
              env:
                  COMPOSER_POLICY_ADVISORIES_BLOCK: ${{ matrix.advisory_blocking }}
              run: composer update --prefer-dist --no-interaction ${{ matrix.composer_flags }}

            - name: Validate composer.json
              run: composer validate --strict

            - name: Run lint
              run: composer lint

            - name: Run tests
              run: composer test
